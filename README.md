# Very basic Claude Code

Instal UV and:
- `uv sync`
- `.\.venv\Scripts\activate`
- `uv run .\mini_claude.py`

OR just use `pip install -r requirements.txt` and `python mini_claude.py`.

See `EXPLAINER.md` for detailed description of how it works.